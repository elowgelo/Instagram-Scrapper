# Development Workflow

## Task Execution & Track Lifecycle
1. **Track Planning**: Every feature or fix is developed in a dedicated track folder under `conductor/tracks/`.
2. **Phase Execution**: Tasks are completed sequentially (Scaffolding -> Scraper Engine -> Keyword Filter -> Web UI -> Data Export).
3. **Verification**: Each step is empirically verified by running local commands/servers before concluding.
