# JavaScript / React Style Guide

- Use functional React components with standard Hooks (`useState`, `useEffect`).
- Follow modern ES6+ syntax (const/let, destructuring, async/await).
- Maintain CSS variables in `index.css` for styling consistency.
- Keep UI components focused and modular.
